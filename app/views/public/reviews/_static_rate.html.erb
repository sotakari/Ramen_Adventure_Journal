<div id="static-rating_<%= review.id %>"></div>
<div class="form-group">
  <label>総合評価</label>
  <div id="all_rating_<%= review.id %>"></div>
</div>

<script>
$(document).on('turbolinks:load', function() {
  let elem = document.querySelector('#all_rating_<%= review.id %>');
  if (elem == null) return;

  elem.innerHTML = "";
  let opt = {
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: "<%= review.all_rating %>",
    half: true,
    readOnly: true,
  };
  raty(elem, opt);
});
</script>

<div class="form-group">
  <label>麺</label>
  <div id="noodle_rating_<%= review.id %>"></div>
</div>

<script>
$(document).on('turbolinks:load', function() {
  let elem = document.querySelector('#noodle_rating_<%= review.id %>');
  if (elem == null) return;

  elem.innerHTML = "";
  let opt = {
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: "<%= review.noodle_rating %>",
    half: true,
    readOnly: true,
  };
  raty(elem, opt);
});
</script>

<div class="form-group">
  <label>スープ</label>
  <div id="soup_rating_<%= review.id %>"></div>
</div>

<script>
$(document).on('turbolinks:load', function() {
  let elem = document.querySelector('#soup_rating_<%= review.id %>');
  if (elem == null) return;

  elem.innerHTML = "";
  let opt = {
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: "<%= review.soup_rating %>",
    half: true,
    readOnly: true,
  };
  raty(elem, opt);
});
</script>

<div class="form-group">
  <label>具</label>
  <div id="topping_rating_<%= review.id %>"></div>
</div>

<script>
$(document).on('turbolinks:load', function() {
  let elem = document.querySelector('#topping_rating_<%= review.id %>');
  if (elem == null) return;

  elem.innerHTML = "";
  let opt = {
    starOn: "<%= asset_path('star-on.png') %>",
    starOff: "<%= asset_path('star-off.png') %>",
    starHalf: "<%= asset_path('star-half.png') %>",
    score: "<%= review.topping_rating %>",
    half: true,
    readOnly: true,
  };
  raty(elem, opt);
});
</script>